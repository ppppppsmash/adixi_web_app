# kh-project と同じ Supabase の「API用」の値（Dashboard → Project Settings → API）
# ※ postgresql://... の DATABASE_URL は使わない。必ず https:// の Project URL をコピー
VITE_SUPABASE_URL=https://xxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9....

# 省略時は「公開済みの最新1件」を自動表示
# VITE_DEFAULT_SURVEY_ID=uuid-of-survey

# 取得できない → SELECT 用 RLS を追加（src/api/survey.ts のコメント参照）
# 送信できない → survey_responses / survey_response_items に INSERT 用 RLS を追加（同上）
